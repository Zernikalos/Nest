/** Clipboard / window role handled by the shell, not CommandService. */
export enum MenuItemRole {
    Copy = 'copy',
    Cut = 'cut',
    Paste = 'paste',
    SelectAll = 'selectAll',
    Quit = 'quit',
    Close = 'close',
}
